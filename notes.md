//Random for getting a random value from 0 to 100: Math.floor(Math.random()*100)



For the import export module, i must have to set the type="module" on the every file.



Whether the given value is checked or not:
#   if(checkBoxQuery.checked) we can check it.


# i must check that tagName must be always the upperCase while checking a event on the js.  


Ways to check whether checkbox clicked or not: if (e.target.matches(".checkBoxCheck"))


When we want to change a child Content with parent:
spanCreate.childNodes[0].textContent=newInput;


if i only pass a: createButton(e)  it can't catch a e event target so 
we've to use for the Add Event::     selectDiv.addEventListener("click", function (e) { createButton(e) })
